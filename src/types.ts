/* eslint-disable typescript/no-empty-interface */
/**
 * Interface defining the component's options object.
 */
export interface LbxChangeSetsComponentOptions {
    // Add the definitions here
}

/**
 * Default options for the component.
 */
export const DEFAULT_LBX_CHANGE_SETS_OPTIONS: LbxChangeSetsComponentOptions = {
    // Specify the values here
};